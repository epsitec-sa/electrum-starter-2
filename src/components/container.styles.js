'use strict';

export default function (theme) {
  return {
    base: {
      includes: ['resetList'],
      display: 'flex',
      flexDirection: 'column',
      backgroundColor: 'rgb(240,240,250)'
    }
  };
}
